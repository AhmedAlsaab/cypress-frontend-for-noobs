describe('Complete the tasks', () => {
  beforeEach(() => {
    // 1. Create a beforeEach that declines cookies
  });

  it('Validates each navigation destination', () => {
    // 2. Create a scenario which navigates to each of the available languages
    // and validates the URL contains the selected language
  });

  it('Validates the search criteria', () => {
    // 3. 1.Create a scenario which performs a search for the word 'node' via the search bar
    //    2.Intercept the request which fires of the search request
    //    3.Validate it was a successful request
  });
});
